#include <string>

#include <atomic>

namespace Envoy {
namespace Http {
namespace DemoFilter {

template <typename T> class LeapArray {
private:
  /* data */
public:
  LeapArray(/* args */);
  ~LeapArray();

  LeapArray::LeapArray(/* args */) {}

  LeapArray::~LeapArray() {}
};

} // namespace DemoFilter
} // namespace Http
} // namespace Envoy